# 模型兼容性待补测试

## 翻译模型选择

2026-09-08 确认：日常翻译默认推荐 **GPT-5.6 Luna**，用户可在 DSH 会话模型选择器改选 **GPT-5.6 Terra / Sol**，或通过自己的 DeepSeek API Key 使用 **V4 Flash / Pro**。当前翻译沿用会话已选模型，首次配置需手动选择 Luna。

这里的推荐用于首次配置，不宣称各模型的额度消耗或翻译质量已做统一比较；订阅与 API 分别使用各自账号额度。

## 实测记录

记录于 2026-09-07。用户已报告 DSH 内 Luna 调用成功；Spark、GPT-6 Astra 按用户决定留待后续验证。本次兼容性修复不发起这些模型调用。

| 模型 | 现有证据 | 待补验证 |
| --- | --- | --- |
| GPT-5.6 Luna / `gpt-5.6-luna` | 用户报告成功；既有 Windows 验收见 acceptance.md | macOS/Linux 的本人订阅完整流程 |
| GPT-5.6 Terra / `gpt-5.6-terra` | 尚无本项目完整精读通过证据 | 账号可见性、翻译提交、工具往返与继续 |
| GPT-5.6 Sol / `gpt-5.6-sol` | 适配器有模拟测试；不能代替真实模型验收 | 账号可见性、翻译提交、工具往返与继续 |
| DeepSeek V4 Flash / Pro | 可走 DSH 原生模型 API；本表尚无这两个具体版本的完整精读通过证据 | 本人 Key、模型可见性、翻译提交与继续 |
| GPT-5.3 Codex Spark / `gpt-5.3-codex-spark` | 尚无本项目真实调用通过证据 | 账号可见性、文本调用、只读工具往返、多轮继续 |
| GPT-6 Astra / `gpt-6-astra` | 尚无本项目真实调用通过证据 | 账号可见性、文本调用、只读工具往返、多轮继续 |

模型是否出现以本人账号返回列表为准；可见不等于调用或工具调用已通过。OAuth 本地启动及未登录检查也不能代替上述实测。
